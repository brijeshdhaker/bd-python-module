[build-system]
requires = ["uv_build>=0.11.21,<0.12"]
build-backend = "uv_build"

#[build-system]
#requires = ["setuptools>=61.0.0"]
#build-backend = "setuptools.build_meta"

[project]
name = "api_module"
version = "0.1.0"
description = "API service module"
readme = "README.md"

#requires-python = ">=3.8"

dependencies = [
    "fastapi>=0.95.0",
    "core_module", # References the other local package
]

#[tool.setuptools.packages.find]
#where = ["src"]

[tool.uv.sources]
core-module = { workspace = true } # Tells uv to look inside the workspace
