[build-system]
requires = ["uv_build>=0.11.21,<0.12"]
build-backend = "uv_build"

[project]
name = "dbx-module"
description = "dbx-module"
version = "0.1.0"
readme = "README.md"

dependencies = [
    "databricks-connect>=16.4.1",
    "databricks-dlt",
    "databricks-langchain>=0.19.0",
    "databricks-sdk[notebook]>=0.114.0",
    "databricks-sql-connector[pyarrow]>=4.2.6",
    "databricks-sqlalchemy>=2.0.9",
    "nbformat"
    #"unitycatalog-ai[databricks]>=0.3.2",
]


[tool.uv.sources]